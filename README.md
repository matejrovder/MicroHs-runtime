After compilation using `tsc`, you can run `node dist/parser.js` and enter a lambda expression (followed by enter and Ctrl-D/EOF symbol) which will then be eta-reduced and compiled to SKI combinators
